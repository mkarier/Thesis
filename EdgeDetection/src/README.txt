There is a make file to install everything. Then you run with './image filename'

You can add change preprocessing methods in the EdgeDetection.cpp file. There is a spot to change method names in an array. 

Also the controls will appear on 'controllWindow' window but it will change the image on the other window the 'final picture'. 


